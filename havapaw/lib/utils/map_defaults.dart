import 'package:latlong2/latlong.dart';

// Neutral world view used before any location is available.
const LatLng kDefaultMapCenter = LatLng(20.0, 0.0);
const double kDefaultMapZoom = 2.5;
const double kFocusedMapZoom = 15.0;
